# ============================================================
#   RENTCAR EC — FRONTEND .env
# ============================================================
# Copia como .env y ajusta según el entorno.

# Reto 1: apunta al monolito local
VITE_API_URL=http://localhost:3000/api/v1

# Reto 2: apunta al API Gateway (mismo contrato, diferente host)
# VITE_API_URL=http://localhost:8080/api/v1
